apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flowpay-be-admin
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/Nebojsa92/flowpay-be-admin.git"
    targetRevision: main
    path: chart/
    helm:
      valueFiles:
        - values.yaml
        - values-admin.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: flowpay-be-admin
  syncPolicy:
    automated:
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m