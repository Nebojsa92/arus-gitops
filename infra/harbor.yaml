apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: harbor
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/Nebojsa92/arus-gitops.git"
    path:  infra/harbor/charts/
  destination:
    server: "https://kubernetes.default.svc"
    namespace: harbor
  syncPolicy:
    automated:
      selfHeal: true
      prune: true